import stdio
import stddraw

while True:
	stddraw.show(0)
	if stddraw.hasNextKeyTyped():
		key = stddraw.nextKeyTyped()
		if key =='w':
			stdio.write(key)
		elif key =='s':
			stdio.write(key)
		elif key =='o':
			stdio.write(key)
		elif key =='l':
			stdio.write(key)